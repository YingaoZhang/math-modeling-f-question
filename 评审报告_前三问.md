# 数学建模 F 题前三问 · 评审报告

评审对象：`D:\project`（2026 年中国研究生数学建模竞赛 F 题《算力约束下提升大语言模型能力的资源配置建模》）
评审日期：2026-09-24
评审范围：问题一、问题二、问题三的代码、论文与结果表；所有关键数字均已用项目 venv 实跑复现核对。

---

## 一、总体评价

**结论：这是一份完成度相当高、学术诚信度罕见的参赛作品，但存在 1 个硬伤（第四问缺失）和 3 个需要修正的方法论过度解读。**

优点集中在三点：

1. **数字全部可复现。** 论文里写的每一个关键指标，我都用 `D:\project\.venv` 跑原始 CSV 核对过，没有发现一处编造或前后矛盾。
2. **学术边界意识极强。** 面对"数据不够"的地方，作品选择**明确声明不可识别**而不是硬拟合。例如 A 表 The Pile Loss 与 B 表 Pythia val_loss 没有共同验证集，作品直接列为"不可识别，不虚构桥接式"——这在竞赛作品中非常少见，是明显加分项。
3. **有一个真正漂亮的发现。** 第一问的置换检验：作者用域内独立重排构造零分布，得到"无冲突"基准约 55.7%，而实测冲突率 42.6% **显著低于**该基准。这推翻了自己最初的"冲突普遍存在"假设，得出"各质量维度本质同向"的正确结论。这个零分布 55.61% 在数学上完全正确（5 组独立时 P(无冲突)=2×0.75⁵−0.5⁵=0.4434，故冲突率≈55.7%），是一次成功的自我纠错。

---

## 二、问题清单（按严重程度排序）

### 🔴 P0 — 硬伤

#### P0-1 第四问完全缺失

题目是**四问递进**：Q1 质量与配比 → Q2 广义标度律 → Q3 算力约束优化 → Q4 技术演进与前沿预测。项目只做到前三问。

题面对 Q4 的要求是明确的、且**附件齐全**：
- 用附件 C（`C1/C2` 排行榜、`C3`、`C4` 的算力/数据量/开源字段）建立动力学或因果模型；
- 分离**规模扩张**与**非规模技术进步**的贡献占比；
- 在算力增长放缓情景下预测未来 12/24 个月开源模型能力前沿，并给出不确定性；
- 用 `C6/C5`（`loss_benchmark_bridge_expanded.csv` 就在附件里）建立 Loss→Benchmark 映射。

**目前 `C_efficiency_evolution/` 下的 `leaderboard_cleaned.csv`、`leaderboard_enhanced.csv`、`leaderboard_extended_timeseries.csv`、`epoch_all_ai_models.csv`、`loss_benchmark_bridge*.csv` 完全没有被任何代码使用。** 这是最优先要补的。

补法建议：`L_bridge(Benchmark)` 用附件自带的桥接表做非线性（如 sigmoid / 对数）映射；前沿预测用"规模项 + 时间趋势项"的分解，时间趋势项即非规模技术进步贡献；不确定性用分位数回归或 bootstrap。

---

### 🟠 P1 — 方法论过度解读

#### P1-1 Q3 的最优解退化为边界解，但结论被拔高成"工业规律"

实跑确认：**在全部 3 档预算 × 3 种成本函数 × 5 个上下文 = 45 个情景里，Q_target 恒等于 0.95 上界**；高预算下 N 恒贴 1000B 上界。原因是质量成本只占预算的 **0.05%–14%**（`quality_cost_share`），而质量收益项 γ(1−Q_eff)^θ 单调递减，所以"把质量拉满"几乎免费。

论文在正文里诚实地说明了这是边界伪影（"属于边界设定与成本函数共同造成的情景结果"），**但在第 7 节结论却写成**：

> "长上下文算力挤压高度非对称，应大幅削减参数规模而优先保数据吞吐"

这句话是从一个**质量项已被拉到上界、注意力项又是唯一约束**的退化模型里推出来的，不能作为一般性结论。建议：结论段降级为"在本文校准下/本代理模型下"，或直接删掉这句因果性表述。

**根本修复**：附录 B 的三类 g(Q) 里，指数型和幂函数型在 Q→1 时增量极小，是导致 Q 贴边的直接原因。应当**把 g(Q) 归一化到"Q=1 总成本 = 训练成本的一个可观比例（如 5%–20%）"**，让质量维度真正进入内点权衡区，再重跑。作品已经做了 `quality_cost_scale` 网格（0.3→10000）并定位到转折点在 scale≈30000，说明作者意识到了问题，只是没有把它作为主口径。

#### P1-2 Q3 自称"禁止跨口径拼接"，实际混用了两套口径

`q3_data.py::_read_coefficients` 里，主口径 `b6_b8_lambda10` 的 (A,B,α,β) 来自 **B6–B8 半合成补充集**的拟合，而对照口径 `b1` 的 (A,B,α,β) 来自 **B1 真实 Pythia 日志**——但**两套口径的质量项 (γ, θ) 是同一个 λ=10 的 B6–B8 值**（代码里 `{"b6_b8_lambda10": quality10, "b1": quality10.copy()}`）。

也就是说，B1 口径是"真实 B1 标度参数 + B6–B8 半合成质量项"的拼接，这恰恰违背了论文第 1 节"不把跨数据口径的系数拆开互换"的声明。而题材明确要求"附件 B 中的 B6–B8 为半合成补充集……**不得将其表述为直接实验观测**"。

建议：B1 口径要么只报告经典标度律、不挂质量项；要么明确标注这是"情景拼接"并说明理由。同时，主口径把 B6–B8（半合成）当作**唯一**质量证据也不妥——正文应显著强调其半合成属性。

#### P1-3 Q1 的 Q̄(p) 接口在 Q3 里基本不可用，但仍在驱动主结果

`q2_input_interface_bundle.json` 显示：**17 个域里 14 个的 Q_star 是中位数插补值 0.5425**，只有 arxiv(0.7323)、github(0.4057)、stackexchange(0.5425) 是实测。`qbar_imputation_variance_loss.csv` 自己算出插补使 Q̄ 方差相对"仅观测域重闭合"口径**损失 91.4%**。

后果：Q3 里 `Q_eff = 1 − (1−Q_target)·Q0/Q̄(p)`，而 Q̄(p) 对 14/17 个域是常数 → **配比 p 对质量项几乎没有任何区分度**，第 4.5 节"配比重塑"得到的所有倍数（enron 11.6×、pile_cc 0.077×）实际主要由 **KL 惩罚项和 t 向量**驱动，而不是质量。

作品对此有自认（"Q̄ 只作为弱证据"），这是好的；但 Q3 的主要输出——配比表——因此说服力不足。建议在 Q3 明确做一次"Q̄ 冻结为常数"的对照实验，证明配比结论不依赖这个弱接口。

---

### 🟡 P2 — 结构性与表述问题

#### P2-1 Q1 的 17 域配比 vs 7 域质量，跨体系关联太弱

`q_domain_summary.csv` 只有 **7 个文档域**：arxiv / book / commoncrawl / stackexchange / c4 / wikipedia / github。
而配比建模有 **17 个组成域**（The Pile 子领域），其中 13 个有 Loss。

题面明确说"二者的域分类体系不同，若需将质量信息引入配比建模，应自行建立跨体系关联并说明规则与假设（A16 给出参考映射）"。作品的处理方式是把 7 域质量**中位数插补**到 17 域 → 相当于放弃了跨体系关联。A16（`domain_mapping_guide.csv`）也没有被真正用起来。

这是 Q1 里最实质的短板。建议：至少用 A16 把 7→17 的映射显式列成表，能映射的用映射值，映射不上的标注为"未覆盖"，而不是一把插补成中位数。

#### P2-2 ILR 建模只针对 13 个有 Loss 的域，但 4 个无 Loss 域仍影响系数

`ilr_domain_semantics.csv` 的说明是对的（ILR 系数是相对替代效应），作品也点出了"4 个无对应 Loss 的组成仍会通过闭合与基准对比影响系数"。方向正确，但**没有做剔除这 4 个域的稳健性对照**。对一个 512 样本、16 维的设计来说，这种对照是必要的。

#### P2-3 跨尺度 R² 已崩坏到 −600，正文表述偏轻

`loss_macro_metrics.csv` 实测：

| split | 宏平均 R² |
|---|---|
| test (1M) | 0.7641 |
| test_60m | **−7.97** |
| test_1b | **−606.19** |

正文写的是"跨到 60M 与 1B 时绝对 R² 显著恶化"。"显著恶化"太轻了——这是**彻底失效**（R² 为负意味着还不如取均值）。虽然作品的结论（绝对 Loss 不可跨尺度迁移）是对的，但数值应该直白给出，这反而是个有说服力的边界声明。

#### P2-4 交付物分裂成两套文档

目前是：`outputs/数学建模F题统一论文.docx`（Q1+Q2）+ `outputs/q3/第三问完整论文.docx`（Q3 单独）。
- 两套文档的模型口径、符号体系、数据边界声明**各自表述**，容易出现例如"Q_star 定义"在两处的细微不一致；
- Q3 文档标题只写"数学建模 F 题"，没说明它在四问递进中的位置；
- README 里写"两问完整结果"，与实际的"三问"不符（README 明显是早期版本没更新）。

建议合并为单一一篇四问论文，README 同步更新。

#### P2-5 README 与 HARDCODE 的环境路径

`README.md` 里写：
```
C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe build_unified_docx.py
```
这是**某台机器上的绝对路径**，换机必然失败。应改为 `python build_unified_docx.py`。

#### P2-6 `requirements.txt` 是空文件

0 字节。（不过 `pyproject.toml` 里有依赖声明，可接受，但空文件容易被误判为遗漏。）

---

## 三、逐问亮点（含可复现证据）

### 问题一

| 项 | 论文值 | 实跑复核 | 结论 |
|---|---|---|---|
| 线性 Ridge 宏平均 R² | 0.7641 | 0.764079 | ✅ |
| 二次交互 Ridge | 0.8628 | 0.862826 | ✅ |
| Data Mixing Laws 指数式 | 0.8688 | 0.868843 | ✅ |
| CV 选型后宏平均 R² | 0.8896 | 0.889597，13/13 为正 | ✅ |
| 置换零分布均值 | ≈55.7% | 0.556103 | ✅ |
| A1/A2/A3 冲突率 | 42.6/53.7/47.9% | 0.4255/0.5366/0.4791 | ✅ |

**真正的技术亮点**：
- 用 **Helmert 正交基做 ILR**（Aitchison 成分数据框架），并对配比零值做**乘性替换**再闭合，而不是简单加常数——这是成分数据里的正确做法。
- **模型选型只在训练集 CV 上做，独立 1M 测试集只用一次**。这个流程纪律是对的，避免了"在同一测试集上挑最好模型"的常见陷阱。
- 二次交互项用 CV 选 Ridge 强度收缩 152 个二阶设计项，且**明确声明 65 条二阶差分是"模型条件非加性"而非"实验因果协同"**——表述精准。

### 问题二

| 项 | 论文值 | 实跑复核 | 结论 |
|---|---|---|---|
| A / B | 2.0824 / 1.2552 | 2.082391 / 1.255165 | ✅ |
| α / β | 0.0661 / 0.2762 | 0.066134 / 0.276249 | ✅ |
| β 先验 / 惩罚 λ | 0.2799 / 10 | 0.279878 / 10.0 | ✅ |
| γ / θ（λ=10） | 1.5165 / 1.0868 | 1.516492 / 1.086828 | ✅ |

**真正的技术亮点**：
- **两阶段拟合切断截距与指数的尺度抵消**。这是对 Kaplan/Hoffmann 标度律拟合里一个真实病态（E 与 A、B 的共线性）的正确处理。先固定 β=0.2799（B1 先验），再用 λ=10 的惩罚项局部松弛，并报告 β 的偏离量而非宣称 "R² 提升"。
- **质量项设计有物理符号保证**。作者发现 B6–B8 里 Q_score（缺陷率）与 Loss 同向，于是构造 `L = A N^-α + B D^-β + γ·Q_score^θ`（γ,θ>0），并证明：
  - `∂L/∂Q_quality < 0`（质量越高损失越低）✅
  - `Q_quality → 1` 时缺陷项 γ·Q_score^θ → 0，**自动退化为经典标度律** ✅
  - 还专门验证了"镜像变换 |Q_score−1| 与 1−Q_score 只差保号仿射，不能翻转方向"（`q2_quality_mirror_equivalence.csv` 里两者预测差恒为 0）——这是个很细致的一致性检查。
- **MRTS 推导正确**。对 L(N,D,Q) 全微分并施加 Σdp=0，得出"质量 +0.1 等价于参数减少 34.8%–75.8%"（中位数 56.5%），并给出倒数（1.53×–4.13×）表示"靠堆参数达到同等降损所需的倍数"。量纲和方向都对。

### 问题三

| 项 | 论文值 | 实跑复核 | 结论 |
|---|---|---|---|
| 单位换算 6ND / 1e-12 | — | 0.006·81.06·19.15=9.312（1e21） | ✅ |
| Lctx_crit = 6/η | 30000 | 6/(2e-4)=30000 | ✅ |
| 三档预算 | 1e19/1e22/1e24 | 0.01/10/1000 (×1e21) | ✅ |
| 情景总数 | — | 2 口径 × 3 预算 × 5 上下文 × 3 成本 = 90 行 | ✅ |

**真正的技术亮点**：
- **单位换算链条完全自洽**，且代码里用注释显式说明了"billion tokens × 1e-12 = 1e21 FLOPs"这一步，没有常见的量纲错误。
- `Lctx_crit = 6/η` 是题面要求的解析临界值，作品**严格按定义推导并做了 η 敏感性**（η 从 1e-4 到 8e-4，临界长度 7500–60000），没有主观给定。
- **不确定性传播做得实**：对六参数抽样 200 次（`q3_uncertainty_samples.csv` 有 1200 行）传播到 N、D、Q 和三类成本份额，给 2.5%–97.5% 分位区间。
- **上下文敏感性结果本身是有价值的**：attention_cost_share 从 2048 时的 6.4% 涨到 131072 时的 81.2%，N 从 81B 压到 34B。这个"注意力开销挤压参数规模"的定性趋势即使模型退化，也是合理的机制观察——只要表述不越界成"工业定律"。

---

## 四、优先修复清单

按投入产出比排序：

1. **补做第四问**（P0）。附件全在，是最大的失分点。
2. **Q3 重做质量成本校准**（P1-1）：把 g(Q) 归一化到可观占比，让 Q 脱离 0.95 上界，得到真正的内点解；然后结论段要么有内点支撑、要么降级表述。
3. **统一 Q3 的参数口径**（P1-2）：B1 口径不要挂 B6–B8 的质量项，或显式标注为拼接情景；主口径强调 B6–B8 的半合成属性。
4. **Q1 用 A16 做显式 7→17 域映射**（P2-1），替代全盘中位数插补；至少把映射表列出来。
5. **补一个"剔除 4 个无 Loss 域"的稳健性对照**（P2-2）。
6. **合并两套论文为单篇四问论文，README 同步更新并去掉硬编码路径**（P2-4、P2-5）。
7. 正文把跨尺度 R²=−606 直白写出（P2-3）；`requirements.txt` 补内容（P2-6）。

---

## 五、一句话总结

**前三问的建模功底、自我纠错能力和数据边界诚信，都在竞赛作品的上游水平；真正的问题不是做得不好，而是"少做了一问"+"把自己推导出的边界伪影说成了普遍规律"。把第四问补上、把 Q3 的质量校准修好、把结论措辞收回半步，这份作品就完整了。**
