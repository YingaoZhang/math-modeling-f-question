# 《数学建模 F 题完整论文》统一修改意见

> **合并来源**：`评审报告_前三问.md`（2026-09-24）、`第四问风险清单.md`（2026-09-25）、`完整论文评审_四问.md`（2026-09-25）
> **评审对象**：`outputs/数学建模F题完整论文.docx`（5.99 MB / 34 页）+ `src/f_question/` 全部代码 + `outputs/` 全部结果表
> **核查方式**：项目自带 `.venv` 逐项实跑复现；附件 C 原始文件（含 C8 的 1958 个 JSON）现场解析；与题目原文、《数据说明》附录 A 逐条对照。**本文引用的每一个数字均有磁盘产物出处，见 §7。**

---

## §0　总判断

**一句话**：建模功底、学术诚实度、可复现性三项都在竞赛作品的**上游**；失分全部集中在**第四问的答题合规性**和**全文的交付形态**上。这不是"做得不好"，而是"红线没对齐 + 收口没做完"。

| 维度 | 评级 | 依据 |
|---|---|---|
| 建模思路 | **优** | 四问递进链条完整；Q1 置换检验自我纠错、Q2 两阶段拟合切断截距-指数抵消、γ·Q_score^θ 保证 Q→1 退化经典式、Q4 的 High/Medium 分层桥接，均为正确且有想法的设计 |
| 计量严谨性 | **优** | 全部关键数字可复算，无一处编造或前后矛盾（见 §7 索引） |
| 学术诚实度 | **优** | R²=0.158、p=0.377、slope CI 含 0、bootstrap 区间 ±2.3、"支持域外"逐行标注、B9/B10 偏置显式隔离——不确定性标注密度远超一般竞赛论文 |
| 代码与可复现性 | **良** | 接口有边界文件、原始数据不回写输出目录、损坏数据留痕；扣分项是 README 硬编码绝对路径 |
| **答题合规性** | **差** | 题目附录 A 对问题四的 3 条"须"式硬要求（C3 / C4 三字段 / C8 逐任务）**只满足 1 条** |
| **结论可靠性** | **弱** | Q4 核心数字 6.48 分 100% 落在桥接支持域外、来自可行域角点，且量级上等价于 Pythia-12B |
| **交付完整性** | **一般** | 三篇论文机械拼接，无四问一体总摘要、无目录、标题层级断裂 |

**改动量预估**：P0 四条（红线）约 1 个新脚本 + 3 处小改 + 1 节重写；P1 四条以"改写段落 + 补一个包络模型"为主；P2 五条基本是排版与文字。**没有需要推翻重做的部分。**

---

## §1　修改总览（统领表）

| # | 优先级 | 动作 | 为什么（题目/风险依据） | 成本 | 验收标准 |
|---|---|---|---|---|---|
| **A** | 🔴 P0 | C8 逐任务聚合分析 | 附录A 问题四："**须对 C8 做至少一项逐任务聚合分析，不得仅使用汇总表**"——现为 0 使用 | 低（1 脚本 + 1 表 + 1 图） | 产出 `model × task` 矩阵，含各任务时间斜率与规模斜率，写进 Q4 新一节 |
| **B** | 🔴 P0 | 接入 C3 做时间轴 | 附录A："**须使用 C1（或 C2）与 C3**"——C3 已 `read_csv` 进内存但零消费 | 低 | `year_coef` 由 C3 `Year` 给出，并显式交代时间轴口径 |
| **C** | 🔴 P0 | 补 C4 数据量与开源权重字段 | 附录A："**须使用 C4 中的算力、数据量、开源权重等字段**"——现只用了算力 | 低 | 提取并分析 `Training dataset size (total)` 与 `Open model weights?`；新增"开源筛选口径"小节 |
| **D** | 🔴 P0 | 贡献占比显式化 | 题目题眼："**分别计算规模扩张与非规模技术进步各自的贡献占比**"——现无任何占比数字 | 低 | 给出占比 + 定义 + bootstrap 区间 + 残差占比 |
| **E** | 🟠 P1 | 6.48 分重定性 + 宏观经验包络 | 6.48 等价于 Pythia-12B 水平，作为"前沿预测"不可交付 | 中 | 改述为基座保守下界；补 C3/C4 经验包络的预测带 |
| **F** | 🟠 P1 | 角点解 + 支持域外写进结论 | 防被判"表里标注、结论隐瞒" | 极低 | Q4 §7 与摘要明写两句话 |
| **G** | 🟠 P1 | 桥接有界化，或降级为交叉校验 | slope CI 含 0 却用于定量换算；外推区间宽 4.58 分 | 中 | 换 logistic/有界形式，或改在 `S ~ log10(C)+month` 空间建前沿 |
| **H** | 🟠 P1 | Q3 结论降级 + α 口径依据 | α 两套差 3.5 倍直接决定 Q4 最核心数字 | 中 | 结论改条件性表述；给出 α 依据或双口径包络 |
| **I** | 🟡 P2 | 全文融合为统一参赛终卷 | 交付形态 | 中 | 单一总标题 + 规范多级标题 + 千字总摘要 + 目录 |
| **J** | 🟡 P2 | 格式硬伤清理 | 观感 | 极低 | 见 §4-J |

---

## §2　P0：题目红线，不修直接失分

### A　C8 逐任务聚合分析（当前完全缺失）

**题目依据（附录 A · 问题四原文）**：

> 须对 C8 做至少一项逐任务聚合分析，不得仅使用汇总表。

**现状证据**（`outputs/q4/q4_data_quality_audit.csv` 实测行）：

```
C8 detailed_results/*.json, 1958, 0, 0, "all excluded; malformed files listed"
```

即：1958 个 JSON 被逐个做**语法解析**后**全部排除**。`q3_data.py:159` 更明写：

```python
{"item": "C8 detailed_results", "role": "not used in Q3 or Q4"}
```

论文还专门写了一节 + 一张 440 KB 排除清单（`q4_json_exclusion_manifest.csv`）+ 一张附录表来证明自己排除了 C8——**等于把"我没做要求的那件事"写成了工作亮点**。

**C8 的实际结构（已现场解析确认，`detailed_results/<model>/results_<ts>.json`）**：

| 字段 | 内容 |
|---|---|
| `model_name` | 如 `01-ai/Yi-1.5-34B` |
| `date` | Unix 时间戳（如 1718742335.35 → 2024-06-19） |
| `results` | `任务名 → {acc_norm,none / acc,none / exact_match,none / prompt_level_strict_acc,none …}` |
| `groups` | 分组级聚合结果 |
| `group_subtasks` | **官方层级**：`leaderboard` → 7 个子组 |

`group_subtasks['leaderboard']` 给出 7 个官方子组：

```
leaderboard_arc_challenge, leaderboard_musr, leaderboard_ifeval,
leaderboard_math_hard, leaderboard_gpqa, leaderboard_bbh, leaderboard_mmlu_pro
```

每个子组还带完整子任务（如 `leaderboard_bbh` → 24 个 BBH 子任务；`leaderboard_math_hard` → 7 个数学子任务；`leaderboard_gpqa` → main/diamond/extended）。

**落地规格（新增 `src/f_question/q4_task_aggregation.py`）**：

1. 遍历 1958 个 JSON，读 `model_name` / `date` / `groups` / `group_subtasks`；
2. 对 7 个顶层子组按指标优先级取值：
   `leaderboard_ifeval → prompt_level_strict_acc,none`；其余 6 组 → `acc_norm,none`；缺失时回落 `acc,none` / `exact_match,none`；
3. 输出 `outputs/q4/q4_task_scores.csv`：`model, date, group, score`（长表，可透视成 `model × task` 矩阵）；
4. 关联 C4 的 `Parameters` 与 `Training compute (FLOP)`（按 `model_name` 归一化匹配）；
5. 对**每个任务**分别回归 `score ~ log10(Params) + months_since_anchor`，输出
   `outputs/q4/q4_task_heterogeneity.csv`：`group, n, time_slope, time_slope_ci, scale_slope, scale_slope_ci, r2`；
6. **论文新增 Q4 一节《非规模技术进步的任务异质性指纹》**：若各任务的 `time_slope` 存在显著差异，即为"非规模技术进步是任务结构性的"直接证据。

**为什么这一条价值最高**：它同时解决三个问题——① 补上唯一被明文强制却完全缺失的要求；② 把 Q4 目前"一条线性时间项"的贫弱分解撑成任务级证据；③ 给"非规模技术进步"提供可解释的载体（哪些任务在涨、哪些停滞）。

**顺带修正**：把 `q4_data_quality_audit.csv` 的 `C8` 行从 `all excluded` 改为 `1958 parsed; 1954 aggregated; 4 malformed (0.20%)`——4 个损坏文件是**0.2% 的损失**，不是"全部排除"。

**旁证（已有数据即可支撑，无需等脚本）**：C3 的六个任务维度逐年最大值显示明显异质性——

| 年份 | IFEval | BBH | MATH_Lvl5 | GPQA | MUSR | MMLU_PRO |
|---|---|---|---|---|---|---|
| 2024 | 89.98 | 65.47 | 62.54 | 24.94 | 38.69 | 70.03 |
| 2025 | 85.78 | 76.70 | 71.45 | **29.42** | 38.69 | **57.26** |

IFEval 已近饱和甚至回落（89.98→85.78），而 **GPQA 仅 29.42、MUSR 仅 38.69**，两者是最明显的"未饱和任务"。这就是任务异质性指纹的雏形。

---

### B　接入 C3（已加载但从未使用）

**题目依据**：附录 A——"须使用 C1（或 C2）**与 C3**"。

**现状证据**：`C3 = leaderboard_extended_timeseries.csv` 在 `q4_data.project_paths` 里被 `read_csv` 加载进内存，**全文/全代码没有任何一处消费它**。"已加载未使用"在代码审查里比"没加载"更显眼。

**C3 的实际结构（实测 4599 行）**：

```
Model, Year, Params_B, Average, IFEval, BBH, MATH_Lvl5, GPQA, MUSR, MMLU_PRO, Source
GPT-2 (1.5B), 2019, 1.5, 5.00, ...
GPT-3 (175B), 2020, 175, 50.00, ...
```

**这一张表一次性满足三项要求**：

1. **补上"须使用 C3"**（本条）；
2. **时间轴口径**——可用 `Year` 明确交代"按发布年份对齐"，并写明为何不用 C1 的 `Submission Date`（后者实测只有 2024-06-08 ~ 2025-03-13，约 9 个月，比"当前"早 18 个月）；
3. **六维分项**——`IFEval / BBH / MATH_Lvl5 / GPQA / MUSR / MMLU_PRO` 与 C8 的 7 个 leaderboard 子组同源，可直接支撑 §A 的任务级分析。

**落地**：把 `year_month` 解释变量由 C1 的 Submission Date 改为 C3 的 `Year`（或做双口径对照，两者都报）；在论文"时间轴口径"小节给出选择理由。

---

### C　补 C4 的数据量与开源权重字段

**题目依据**：附录 A——"须使用 C4 中的**算力、数据量、开源权重**等字段"。

**现状证据**（`q4_data_quality_audit.csv`）：C4 只取了 `Training compute (FLOP)`，而且论文自述"C4 的训练 FLOP 主要用于覆盖审计"。**题目点名的另外两个字段完全未提取。**

**C4 的实际结构（实测 3523 行 × 57 列，已确认列索引）**：

| 列 | 列名 | 非空行数 | 现状 |
|---|---|---|---|
| [13] | `Training compute (FLOP)` | 1390 | ⚠️ 仅用于覆盖审计 |
| **[15]** | **`Training dataset size (total)`** | **1424** | ❌ 未提取 |
| **[56]** | **`Open model weights?`** | **2653** | ❌ 未提取 |
| [49] | `Frontier model` | **137** | ❌ 未使用（**可定义"前沿"**） |
| [5] | `Publication date` | — | 未使用（比 C1 的 Submission Date 更规范） |
| [46] | `Training compute cost (2023 USD)` | — | 未使用 |
| [25]/[43] | `Model accessibility` / `Training code accessibility` | — | 未使用（**开源筛选口径的依据**） |

**落地规格**：

1. `prepare_epoch` 提取 `Training dataset size (total)`、`Open model weights?`、`Frontier model`、`Publication date`、`Model accessibility`；
2. **新增 Q4 小节《开源筛选口径》**，必须回答三问：
   - 是否要求开源权重？→ 用 `Open model weights?`（覆盖 2653/3523 = 75.3%），**优于** C2 的 `Epoch_AI_Open_Weights`（实测只覆盖 443/4576 = 9.8%）；
   - 时间口径取哪个？（Publication date / C1 Submission Date / C3 Year，三选一并给理由）；
   - 许可证是否允许研究与复现？→ 用 `Model accessibility` + `Training code accessibility` 交代，若数据不足则**明说不足**（这也是一种合规回答）。
3. **顺带解决"前沿"的定义问题**：Q4 现在把 `C4 最大观测算力 5.000e26` 当作前沿起点，却没说那是哪个模型。C4 有 `Frontier model` 列（137 行非空）——用它定义前沿，并交代是否开源、何时发布。
4. **数据量与算力的关系**可顺手做一个交叉验证：C4 同时有 `Training compute (FLOP)` 与 `Training dataset size (total)`，可用 `6ND` 反推 N、或核对 Q2 标度律在真实数据上的表现。**这是把 C4 从"覆盖审计"升级为"独立外部校验"的最低成本路径。**

---

### D　贡献占比显式化（本题题眼）

**题目依据**：附录 A——"**分别计算规模扩张与非规模技术进步各自的贡献占比**"。

**现状证据**（`outputs/q4/q4_decomposition_summary.csv` 实测）：

```
n_pretrained=330, intercept=7.9375, scale_coef=6.2244, time_coef_per_month=0.0670,
r2=0.3594, scale_sd_contribution=4.6850, time_sd_contribution=0.2221
```

**只给了两个 sd，全篇没有任何形式的"占比"数字。** 而"占比"是题目通俗理解的题眼原话。

**更要命的是：占比换一种定义答案就翻脸。**

| 占比定义 | 规模扩张 | 非规模（时间） |
|---|---|---|
| 按 **sd 之比**（论文隐含口径） | **95.5%** | **4.5%** |
| 按 **方差之比** | **99.78%** | **0.22%** |

同一组系数、同一个模型，一种定义说"非规模占 4.5%"，另一种说"占 0.22%"——**差 20 倍**。题目要"占比"，就必须**写出定义、给出区间、说明敏感性**。

**另外两个必须补的事实**：

1. **两者合计只解释 35.9% 的方差**（R²=0.3594），剩余 **64.1% 归入残差**。这个事实论文一个字没提。主动写明反而增强可信度——它诚实地划定了模型能力边界。
2. `q4_models.decompose_scale_vs_time` **没有输出任何标准误或置信区间**，而同一个包里 `q4.py` 的桥接却老老实实做了 2000 次 bootstrap。**同一篇论文内口径不一致**，核心结论（6.2244 vs 0.0670）连显著性都没检验。

**落地规格**：

1. 在 Q4 §4 加一张占比表，**同时列出两种定义**，并声明主口径（建议用方差分解，因为"贡献占比"在方差归因语境下有标准含义）；
2. 对 `scale_coef` / `time_coef` 做 **bootstrap（≥2000 次）**，给出占比的 **95% 区间**；
3. 显式写出：`scale + time 合计解释 R² = 0.3594，残差占 64.1%`，并说明残差包含什么（未观测的数据质量、架构创新、后训练/RLHF、评测噪声）；
4. 补一句口径敏感性：`按 sd 比 → 95.5%/4.5%；按方差比 → 99.78%/0.22%；两者都指向"规模主导"，但非规模部分的大小不可精确识别`。

> **这一条的性价比在所有修改项里排第一**：改动只是一个函数 + 一张表，却直接补上题眼，而且顺带把一个弱点（R² 仅 0.3594）转成诚信加分点。

---

## §3　P1：结论可靠性，不修会被判"表里标注、结论隐瞒"

### E　6.48 分重定性 + 宏观经验包络预测带

**现状证据**（`outputs/q4/q4_frontier_predictions.csv` 实测）：

| horizon | 算力增长 | Loss* | 榜单预测 |
|---|---|---|---|
| 12 月 | 1.25× | 1.3584 | **6.4740** |
| 12 月 | 2.0× | 1.3566 | **6.4756** |
| 12 月 | 4.0× | 1.3566 | **6.4756** |
| 24 月 | 1.25× | 1.3576 | **6.4747** |
| 24 月 | 2.0× | 1.3566 | **6.4756** |
| 24 月 | 4.0× | 1.3566 | **6.4756** |

**三个同时成立的硬事实**：

1. **量级错得离谱。** 6.48（= 六任务百分制均值 6.48 分）恰好等价于 **Pythia-12B 的水平**（`q4_bridge_fit.csv`：pythia-12b 的 `LB_Average = 6.0598`）。而 C3 实测的 2025 年前沿 `Average` 已达 **47.22（p90 = 39.67）**、2024 年最高 **52.08**。**把 6.48 作为 2026-2027 年「能力前沿」交付，等于预测前沿退回到 2023 年的弱基座模型水平。**
2. **100% 域外外推。** 桥接支持域（C6 High 组）`Val_Loss ∈ [2.0933, 2.5978]`，而预测点 `Loss* = 1.357`，**低出下界 0.74，约为 High 组全部观测跨度（0.50）的 1.5 倍**。`outside_high_bridge_loss_support` 列 **6/6 行全为 True**。
3. **12/24 个月无区分度。** Loss* 取值只有 1.3566–1.3584，导致 12 个月与 24 个月的预测差 **0.0016 分**；1.25× 与 4× 的算力差 **32 倍**，结果同样只差 0.0016 分。

**根因（两层）**：

- **上游**：Q3 的 `Loss*(C)` 在预测区间已经**饱和**。`q3_loss_frontier_curve.csv` 第 120 行实测 `N=5000.0, D=4000.0, loss=1.356588`——**两个上界同时激活**，Loss* 已经不再是"模型算出来的前沿"，而是箱子角点的坐标。
- **下游**：桥接是**弱基座模型（Pythia）**的关系，且斜率本身未显著（`slope=-0.8817, 95%CI=[-4.4269, +0.5349]` 跨过零点）。把弱基座的关系外推到远低于其观测 Loss 的区间，得到的是"弱基座能力下界"，**不是前沿预测**。

**修法（两步，用户已提出的方向是对的，这里给出可操作版本）**：

**第一步：重定性。** 把 6.48 从"前沿预测"改述为：

> **未经指令对齐（non-instruction-tuned）基座能力的保守下界。** 该值由 Pythia 基座桥接外推得到，锚定的是"同一 val_loss 下的基座模型水平"；未来 12/24 个月的前沿若经过 SFT/RLHF 与推理时扩展，实际榜单表现应**显著高于**此下界。

这个重定性**有实测支撑**（High 组 7 个点全部是 Pythia 基座；而 C6 Medium 组 LB_Average 上到 47.98，说明指令对齐版在同一可比口径下可高一个量级），而且它把一个"荒谬的预测"翻译成"一个有意义的保守界"，是加分而非减分。

**第二步：补宏观经验包络，形成预测带。** 现有 `Loss→Benchmark` 链路已被证明是放大器而非信息来源，应**并行**建一条有数据支撑的前沿包络：

- **数据**：C3（4599 行，2019–2025，含 `Year` 与 6 个任务维度）+ C4（1390 条带算力记录）+ C1（4576 行）；
- **做法**：对每年取 `Average` 的 **p90 与 max** 两条曲线（即"主流先进水平"与"当年最好"），得到上包络 `E_hi(t)` 与参考线 `E_lo(t)`；再用算力增长放缓情景（年增 1.25×/2×/4×）外推 12/24 个月，得到**预测带**；
- **实测锚点**（可直接进论文）：2023 max 35.00 → 2024 max 52.08 / p90 35.06 → 2025 max 47.22 / p90 39.67；
- **产出**：Q4 §6 从"一条水平死线"改为"一条保守下界（6.48）+ 一条经验预测带（数量级在 40–55 区间）"，并明说两者口径不同、不可直接相减。

**这一步是整篇 Q4 里收益最大的改动**：它同时解决"6.48 荒谬""12/24 无区分度""100% 域外"三个问题——因为预测带来自**有观测数据的时间轴**，不是来自外推角点。

---

### F　角点解 + 支持域外必须写进结论

**现状**：论文 §5 的表里有"支持域外"逐行标注（诚实），但 **§7 结论和摘要都没说"我们的数字来自可行域角点"**。

**落地（两条，均为极低成本）**：

1. Q4 §7 结论加一句：
   > 需强调：本文的前沿预测值取自 Q3 优化可行域的**角点解**（N=5000B 与 D=4000B 双双触顶），且其 Loss* = 1.357 低于桥接支持域下界 2.0933 约 0.74（约为 High 组全部观测跨度的 1.5 倍），**因此该数值应理解为量级参照而非精确点预测**。
2. 总摘要里也留一句对应表述。

> **这是"自保"性质的一条**：一个本来很诚实的作品，不该因为结论里少写一句话而被判成隐瞒。

---

### G　桥接形式有界化（或降级为交叉校验）

**现状证据**（`outputs/q4/q4_bridge_summary.csv`）：

```
n_high=7, slope=-0.8817, r2=0.1580, f_pvalue=0.3773, residual_rmse=0.3338,
slope_95%CI=[-4.4269, +0.5349], high_loss_range=[2.0933, 2.5978]
```

**问题**：slope 的 95% 自助区间**跨过零点**——"val_loss 越低、榜单分越高"这个**方向**在本样本里都未被证实。而 7 个 High 点本身还非单调（pythia-1.4b 的 Average 6.0085 > pythia-2.8b 的 5.5549）。用它线性外推得到的 6.4756，其 bootstrap 95% 区间是 **[4.9295, 9.5061]**——**宽度 4.58 分**，比 C6 里全部实测 benchmark 的跨度（5.07→6.06，仅 0.99 分）宽 4.6 倍。

**两个可选修法（后者更彻底）**：

1. **换有界/单调形式**：`S = S_max − c·(L − L_min)^γ` 或 logistic，至少保证外推不发散、且天然有上界；
2. **降级 + 平行建模**：既然 Q3 的 `Loss*(C)` 在预测区间已饱和，桥接这一步本身就是放大器而非信息来源 → 直接在 `S ~ log10(C) + month` 空间建前沿（C4 有 1390 条带算力记录可用），**把 Pythia 桥接降级为交叉校验**。这样 Q4 的可交付数字会落在自己有数据的地方，且 CI 大幅收窄。

**论文表述**：无论选哪种，都要把 `slope CI 含 0` 这一点从结果表提升到正文——这是诚实性的体现，不是弱点。

---

### H　Q3 结论降级 + α 口径依据

**H-1　结论措辞（Q3 §7 原文）**：

> 主口径重算后，Q 恒卡在 0.95，质量维度的内点权衡不可识别；高预算 N 上界激活是边界设定和成本结构的共同结果。……由此得到的规律性结论是：**长上下文算力挤压高度非对称，应大幅削减参数规模而优先保数据吞吐**；该判断需用长文本训练计时和收益项补充实验验证。

**前半句刚否定自己，后半句仍是明确的规范性表述。** 必须分开处理：

- **"Q 恒卡 0.95"是伪影**——`q3_quality_cost_curves.csv` 实测：成本放大 1000 倍时质量成本占比也只到 0.648，直到 scale≈3000 才跌出 0.95。即：在题目附录 B 给定的指数型 `g(Q)=10⁷e^{6Q}` 下，"把质量拉满"几乎免费，这是**成本函数与系数标定的联合产物**，不是模型发现的规律。
- **"长上下文挤压参数规模"是站得住的**——主口径 10^22 档下 `Lctx: 2048→32768` 时 **N 从 26.29B 降到 15.23B**（降 42%）、**D 从 58.40B 降到 51.59B**（仅降 12%），**是内点解不是角点解**，"非对称"成立；`Lctx_crit = 6/η = 30000` 与题设公式一致。

**修法**：不删结论，而是把"应大幅削减参数规模"从**规范性主张**降级为**条件性表述**——"在本代理模型与附录 B 给定成本函数下"，并明说 Q 维度不可识别。

**H-2　α 口径依据（直接决定 Q4 最核心的数字）**：

| 口径 | A | B | **α** | β |
|---|---|---|---|---|
| b1（Pythia，真实日志） | 2.0824 | 1.2552 | **0.0661** | 0.2762 |
| b6_b8（半合成，Q3 主口径） | 0.8111 | 0.5641 | **0.2334** | 0.2625 |

**α 相差 3.53 倍**，直接导致同一算力下的最优 N 相差约 3 倍（1e22 档：b1 给 26.29B，b6_b8 给 81.06B）。而 Q4 的核心任务恰是拆解规模扩张贡献——**这个数字会随口径剧烈摆动**。且混合口径取的 α=0.0661 比 Chinchilla/Kaplan 的 ~0.34 小 5 倍，物理上更可疑。

**修法**：
1. 给出 α 的口径选择依据（为什么用 b1 的经典项）；
2. **做双口径包络**：把 1.5×–3× 的摆动幅度做成区间报出来（Q4 占比给区间而非点值）；
3. 说明外推倍数：B1 的 N 只有 8 个取值（跨 0.0705→11.966B），而 Q3/Q4 外推到 N=5000B（**×418**）——这个倍数必须在论文里写出来。

**H-3　两处表述与自身数据不符（会被评委抓）**：

| 位置 | 原文 | 事实 |
|---|---|---|
| Q3 §4.3 | "η=2×10⁻⁴ 为**自设**简化系数" | η=2×10⁻⁴ 是**题目明确给定**的（题目原文："其中 η=2×10−4"） |
| Q3 §摘要 | "高预算 **N 触及人为上界**" | 主口径 b6_b8 @1e24 = **921.2B**（内点）；只有对照口径 b1 才恰好 1000.000000 |

**H-4　Q3 的"禁止跨口径拼接"要落实**：Q3 摘要写"禁止跨口径拼接"，但代码里 b1 口径 = B1 的 (A,B,α,β) + B6–B8 的 (γ,θ)。**缓解事实可用**：`q2_quality_effect_coefficients.csv` 显示 γ、θ 对 λ 几乎免疫（γ 波动 <0.02%，θ 波动 <0.04%）。→ 把 b1 口径改名并显式标注为"**混合口径（A/B/α/β 来自 B1，γ/θ 来自 B6–B8）**"，同时引用上表说明 λ 不敏感性，风险即消。

---

## §4　P2：交付收口（不改得分逻辑，改观感与完整性）

### I　全文融合为统一参赛终卷（用户已提出，此处给结构模板）

**现状**：文档是三篇论文**首尾相接**——最前面的摘要只覆盖 Q1+Q2；Q3、Q4 各自另起摘要与参考文献。**四问一体的总摘要不存在，也没有目录。** 另外文件名写"完整论文"，文内 Title 仍是"统一建模论文"，副标题"语料质量评分与大模型训练标度律"（只描述前两问）。

**落地清单**：

1. **单一总标题**：`算力约束下提升大语言模型能力的资源配置建模`，副标题移除或改为覆盖四问；
2. **千字全局大摘要**（结构模板见 §5）；
3. **规范多级标题**：Q3/Q4 现在用 Title 级样式导致 `doc_get_outline` 返回空、Word 导航窗格失效 → 统一改为 `Heading 1/2/3`，确保四级层级连续；
4. **生成目录**（TOC 域）；
5. **清理模板占位**：Q3、Q4 标题下各留一行孤立的"数学建模 F 题"段落；
6. **统一参考文献**：Q3 的 5 条全用 `[a]` 标记而非数字编号；**第一篇第 5 条是伪文献**——"Data Mixing Laws and RegMix related studies, with model comparison restricted to the supplied A and B attachments."（无作者、无年份、无出处，却占了一个编号），直接删除；
7. **修表格断页**：Q4 §3 的桥接表被切成"表格续表"，`residual_rmse / slope_low / slope_high / intercept_low / intercept_high` 单独成行，阅读困难；
8. **README 去硬编码**：`C:\Users\admin\.cache\codex-runtimes\...\python.exe build_unified_docx.py` → 改为 `python build_unified_docx.py`；同时 README 里"完成两问"的过期表述同步更新；`requirements.txt` 目前是 0 字节，补内容。

### J　格式硬伤速查表

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 1 | 无统一总摘要 | 全文 | 见 §5 模板 |
| 2 | 文件名 ≠ 文内 Title | 全文 | 统一为"完整论文" |
| 3 | 无目录 | 全文 | 插入 TOC |
| 4 | Q3/Q4 用 Title 级样式 | Q3/Q4 标题 | 改 Heading 1 |
| 5 | "数学建模 F 题"占位段残留 | Q3/Q4 标题下 | 删除 |
| 6 | 桥接表硬拆成续表 | Q4 §3 | 调整列宽或转置 |
| 7 | 第 5 条伪文献 | 第一篇参考文献 | 删除 |
| 8 | 参考文献样式混乱 | Q3 参考文献 | `[a]` → 数字编号 |
| 9 | README 硬编码绝对路径 | README.md | 改环境无关写法 |
| 10 | `requirements.txt` 空文件 | 根目录 | 补内容或删除 |

---

## §5　千字全局大摘要

> **结构模板（可直接填数）**。四问一体，约 1000 字，每问 200–250 字 + 一段方法学声明。

**第一段（问题背景与总体思路，约 120 字）**：本文围绕"算力约束下如何配置语料质量、数据配比、参数规模与上下文长度以提升大模型能力"展开，构建"质量评分 → 广义标度律 → 算力约束优化 → 技术演进分解与前沿预测"的四层递进模型。

**第二段（问题一）**：建立 ILR 成分数据回归模型，比较线性 Ridge、二次交互 Ridge 与 Data Mixing Laws 指数式三种形式，交叉验证选型后宏平均 R² = **0.8896**（13/13 域为正）。通过域内独立重排构造冲突零分布，得无冲突基准 **55.61%**，实测 A1/A2/A3 冲突率 **42.55% / 53.66% / 47.91%** 全部低于基准，据此推翻"各质量维度冲突"的初判，结论为**各维度本质同向**。

**第三段（问题二）**：建立广义标度律 `L = A·N^−α + B·D^−β + γ·Q_score^θ`，采用两阶段拟合切断截距与指数的尺度抵消，并以 λ=10 惩罚项局部松弛 β。得 A=**2.0824**、B=**1.2552**、α=**0.0661**、β=**0.2762**、γ=**1.5165**、θ=**1.0868**；该构造保证 `Q→1` 时自动退化为经典标度律。MRTS 分析给出"质量 +0.1 等价于参数减少 34.8%–75.8%"（中位数 56.5%）。

**第四段（问题三）**：在 `C_total = 6ND(1 + ηL_ctx/6) + C_Q` 约束下求解四维最优配置。解析给出注意力与训练开销相等的临界上下文 `L_ctx^crit = 6/η = 30000`，与题设一致。对六参数抽样 200 次传播不确定性。结果显示：**长上下文算力挤压高度非对称**——10^22 档下 L_ctx 由 2048 增至 32768 时 N 由 26.29B 降至 15.23B（−42%）而 D 仅降 12%；质量维度在附录 B 给定成本函数下**内点权衡不可识别**（Q 恒贴 0.95 上界），该结论为**条件性结论**。

**第五段（问题四）**：基于 C1/C3/C8 建立"规模—时间"分解模型（n=330，R²=**0.3594**）。规模扩张与非规模技术进步的贡献占比：按方差分解为 **99.78% / 0.22%**，按标准差比为 **95.5% / 4.5%**，两者均指向**规模主导**，但非规模部分的量级不可精确识别；剩余 **64.1%** 方差归于残差。C8 逐任务聚合显示任务间时间斜率存在异质性（IFEval 近饱和、GPQA/MUSR 显著滞后），构成非规模进步任务结构性的证据。前沿预测给出保守下界 **6.48 分**（基座能力口径）与基于 C3 经验包络的预测带。

**第六段（方法学声明与边界，约 150 字，必须有）**：本文所有不确定性问题均显式标注：桥接斜率 95% 区间 **[-4.4269, +0.5349]** 跨过零点；前沿预测点**低于桥接支持域下界 0.74**，6/6 情景均为域外外推；预测值取自 Q3 可行域**角点解**（N 与 D 双上界激活）；Q2 在 100B+ 尺度存在 **0.3109 nats** 的系统性低估（128 个估算点残差 100% 为正），该偏差不叠加至中心预测。**上述边界均随结果同时报告，未作隐藏。**

---

## §6　验收清单（逐项勾选）

**P0 — 题目红线**
- [ ] A. `q4_task_scores.csv` + `q4_task_heterogeneity.csv` 产出，Q4 新增"任务异质性指纹"一节
- [ ] A. `q4_data_quality_audit.csv` 的 C8 行改为 `1958 parsed; 1954 aggregated; 4 malformed (0.20%)`
- [ ] B. Q4 时间轴改用/对照 C3 的 `Year`，并在正文交代口径选择理由
- [ ] C. 提取 C4 `Training dataset size (total)`、`Open model weights?`、`Frontier model`；新增"开源筛选口径"小节（三问均作答）
- [ ] D. Q4 §4 给出占比表（两种定义）+ bootstrap 95% 区间 + 残差 64.1% 说明
- [ ] D. `decompose_scale_vs_time` 输出标准误/置信区间

**P1 — 结论可靠性**
- [ ] E. 6.48 改述为"未经指令对齐的基座能力保守下界"
- [ ] E. 新增 C3/C4 宏观经验包络预测带（含 p90/max 双曲线与 12/24 个月外推）
- [ ] F. Q4 §7 与总摘要各加一句"角点解 + 支持域外"
- [ ] G. 桥接换有界形式，或在 `S ~ log10(C) + month` 空间重建、Pythia 桥接降级为交叉校验
- [ ] G. 正文明确写出 `slope CI 含 0`
- [ ] H. Q3 §7 结论改条件性表述，并明说 Q 维度不可识别
- [ ] H. 给出 α 口径依据或双口径包络；写明 N 外推 ×418 倍
- [ ] H-3. 修正 η 的"自设"表述；修正"N 触及人为上界"表述
- [ ] H-4. b1 口径改名并显式标注为混合口径

**P2 — 交付收口**
- [ ] I. 单一总标题 + 文内 Title 一致
- [ ] I. 千字全局大摘要（按 §5 模板）
- [ ] I. Q3/Q4 标题改 Heading 样式，`doc_get_outline` 能正常返回层级
- [ ] I. 生成目录
- [ ] I. 清理两处"数学建模 F 题"占位段
- [ ] J. 删除伪文献；Q3 参考文献改数字编号；修桥接表断页；README 去硬编码路径；`requirements.txt` 补内容

---

## §7　实测数字索引（每个数字的出处）

| 数字 | 出处 | 用途 |
|---|---|---|
| 0.8896 / 13 域为正 | `outputs/q1/model_form_comparison.csv` | 摘要 Q1 |
| 55.61% 零分布 / 42.55%、53.66%、47.91% | `outputs/q1/conflict_permutation_test.csv` | 摘要 Q1 亮点 |
| A=2.0824, B=1.2552, α=0.0661, β=0.2762 | `outputs/q2/q2_b1_fit_coefficients.csv` | 摘要 Q2 |
| γ=1.5165, θ=1.0868 | `outputs/q2/q2_quality_effect_coefficients.csv` | Q2 质量项 |
| Lctx_crit = 6/η = 30000 | `src/f_question/q3_models.py` | 摘要 Q3 |
| Lctx 2048→32768: N 26.29→15.23B, D 58.40→51.59B | `outputs/q3/q3_selected_by_budget_and_cost.csv` | §H-1 站得住的结论 |
| Q3 主口径 Loss 0.3556 / 0.5947 / 1.3642 | 同上 | Loss 口径隔离 |
| Q3 对照 b1 Loss 3.3087 / 2.1296 / 1.6739 | 同上 | 桥接可用点 |
| Q3 前沿角点 N=5000, D=4000, loss=1.356588 | `outputs/q3/q3_loss_frontier_curve.csv`（第 120 行） | §F 角点解 |
| 6.4740 / 6.4756 / 6.4747 | `outputs/q4/q4_frontier_predictions.csv` | §E |
| outside_high_bridge_loss_support = True × 6 | 同上 | §E 域外 |
| scale 4.6850 / time 0.2221 / R² 0.3594 / n=330 | `outputs/q4/q4_decomposition_summary.csv` | §D |
| slope −0.8817 / R² 0.158 / p 0.3773 / CI [−4.4269, 0.5349] | `outputs/q4/q4_bridge_summary.csv` | §G |
| High 支持域 [2.0933, 2.5978] / bootstrap CI [4.9295, 9.5061] | 同上 + `q4_frontier_predictions.csv` | §E/§G |
| pythia-12b LB_Average 6.0598 | `outputs/q4/q4_bridge_fit.csv` | §E 量级对照 |
| B9/B10 偏置 0.3109 nats，100% 为正，n=128 | `outputs/q2/q2_large_scale_extrapolation.csv` | 摘要边界声明 |
| C4: 3523 行，算力 1390，数据量 1424，开源权重 2653，Frontier 137 | `data/raw/real_attachments/C_efficiency_evolution/epoch_all_ai_models.csv` | §C |
| C3: 4599 行，2025 max 47.22 / p90 39.67，2024 max 52.08 | `leaderboard_extended_timeseries.csv` | §E 包络锚点 |
| C3 任务维度 2025 最大值：IFEval 85.78 / BBH 76.70 / MATH 71.45 / GPQA 29.42 / MUSR 38.69 / MMLU_PRO 57.26 | 同上 | §A 异质性旁证 |
| C8: 1958 JSON / 1863 模型目录 / 4 损坏 | `detailed_results/` | §A |
| C6: 75 条 → High 7 / Medium 68 | `outputs/q4/q4_data_quality_audit.csv` | 桥接分层 |

---

## §8　如果只能改三件

1. **补 A**（C8 逐任务聚合）——唯一被明文强制、且完全缺失的要求；同时给"非规模进步"提供任务级证据。
2. **补 D**（贡献占比显式化）——直接对应题目题眼，改动最小、收益最大。
3. **补 E+F**（6.48 重定性 + 宏观经验包络 + 结论写明角点解与域外）——把一个"荒谬的预测"转成"有意义的保守下界"，并防止诚实被误判为隐瞒。

**前两件是补题目的硬要求，第三件是自保。** 其余（B、C、G、H、I、J）成本都低，按 §1 表顺序推进即可。

---

*合并时间：2026-09-25 · 依据：三份独立评审报告 + 本次新增的 C8/C3/C4 结构解析与包络实测*
