# 《数学建模F题完整论文》评审报告

评审对象：`outputs/数学建模F题完整论文.docx`（5.99 MB，34 页，2026-09-25 11:46）
评审方式：读取最终 docx 全文（218 节点 / 37 标题 / 27 表）、Q4 全部源码（`q4.py` / `q4_data.py` / `q4_models.py`）、Q4 结果 CSV，并与题目原文、《数据说明》附录 A 逐条比对。所有引用的数字均来自磁盘上的实际产物或实跑复算。

---

## 一句话结论

**第四问补上了，写法上的学术诚实度依旧罕见——但它在数据使用上踩了三条题目的硬性红线，且核心量化结论（6.47）100% 落在支持域之外、来自可行域的角点解。工程完成度很高，答题合规性不及格。**

进步是真实的：上一轮我提的"Q2↔Q4 没有契约"被正面解决了——新增了机器可读的 `outputs/q4_input_contract.json`，把 Loss 口径、桥接列名、混合系数来源、B9/B10 偏置、Q̄ 插补损失全部显式传递，并冻结"只用 C6 High 校准"的策略。这一步做得干净。

---

## 一、硬性要求逐条核对（题目附录 A · 问题四）

题目原文：

> **问题四：须使用 C1（或 C2）与 C3；须使用 C4 中的算力、数据量、开源权重等字段；须对 C8 做至少一项逐任务聚合分析，不得仅使用汇总表。**

| 要求 | 实际 | 判定 |
|---|---|---|
| 须使用 **C1 或 C2** | `leaderboard_cleaned.csv`（C1）作为规模—时间回归主表，4,576→333→330 条 | ✅ |
| 须使用 **C3** | C3 = `leaderboard_extended_timeseries.csv`，在 `q4_data.project_paths` 里被 `read_csv` 加载，**但全文/全代码没有任何一处消费它** | ❌ |
| 须使用 C4 的**算力**字段 | 只取 `Training compute (FLOP)`，且论文自述"C4 的训练 FLOP 主要用于覆盖审计" | ⚠️ 形式使用 |
| 须使用 C4 的**数据量**字段 | 表内有 `Training dataset size (total)` 列，`prepare_epoch` 未提取、未使用 | ❌ |
| 须使用 C4 的**开源权重**字段 | 表内有 `Open model weights?` 列，未提取、未使用 | ❌ |
| **须对 C8 做至少一项逐任务聚合分析，不得仅使用汇总表** | `q4_data.audit_c8_json()` 遍历 1958 个 JSON（1863 个模型目录）**逐个只做语法解析**；`q3_data.py:159` 明写 `{"item": "C8 detailed_results", "role": "not used in Q3 or Q4"}`；`q4.py:51` 的审计行 policy = `all excluded; malformed files listed` | ❌❌ |
| 区分 pretrained 与 chat/finetuned | `prepare_leaderboard` 用 `contains("pretrained") & ~contains("fine-tuned|chat|merge")` | ✅ |
| 桥接须**按可比性等级区分使用** | High（7 条）只做校准，Medium（68 条）只做排序核验，等级字段随表保留 | ✅✅ 做得最好的一处 |
| 须**讨论映射误差对结论的影响** | §3 + §5 给出 bridge bootstrap CI，§6 另列 B9/B10 同表偏差敏感性 | ✅ |
| 须说明**综合能力度量方式** | 直接取榜单 `Average`，未说明六维如何归一/加权、为何用平均 | ⚠️ |
| 须说明**开源筛选口径**（是否须开源权重、许可证是否允许研究与复现） | 全文检索"开源 / 许可证 / 权重"→ 0 处相关表述 | ❌ |
| 须说明**时间轴口径**（提交日/发布日/版本日） | 只出现一次"提交日期"，无口径选择理由 | ⚠️ |
| 须**分别量化规模扩张与非规模技术进步的贡献占比** | 给出系数与两个 sd（4.6850 / 0.2221），**没有换算成任何形式的"占比"** | ❌ |

**七条 ❌ 里，C8 那条是硬伤里最硬的**：题目用一个"须…不得仅使用汇总表"的双重强调句把 C8 逐任务聚合设为必做项，而论文专门写了一节 + 一张 440 KB 的排除清单 + 一张附录表来证明自己**把 C8 全部排除了**。等于把"我没做要求的那件事"写成了工作亮点。而 C8 手上有 1958 个 `results_*.json`、覆盖 1863 个模型，是全文唯一能支撑"逐任务"粒度的资源——它同时也是**回答"非规模技术进步体现在哪些任务上"的唯一素材**（若 MATH/GPQA 涨得比 IFEval 快，那本身就是非规模进步的指纹）。

---

## 二、P0：不改会直接扣分的四件事

### P0-1　C8 逐任务聚合分析缺失（题目明文要求）

- 现状：0 使用。审计表写 `rows=1958, filtered_rows=0, model_rows=0`。
- 最小合规做法（成本极低，1 个脚本）：按 `task` 字段把 1958 个 JSON 聚合成 `model × task` 得分矩阵，产出 ①各任务分数随时间的斜率，②各任务分数与 `log10(Params)` 的斜率。**如果任务间斜率显著不同，就直接构成"非规模技术进步是任务结构性的"证据**，能把 Q4 目前"一条线性时间项"的贫弱分解撑起来。
- 顺带把 4 个损坏 JSON 的排除写成"4/1958 = 0.2% 的损失"，而不是现在这种"我排除了全部"的表述。

### P0-2　C3 被加载但从未使用

- `leaderboard_extended_timeseries.csv` 含 `Model, Year, Params_B, Average, IFEval, BBH, MATH_Lvl5, GPQA, MUSR, MMLU_PRO`。
- 这一张表**同时**能满足三项要求：用 C3（P0-2）、时间轴口径可用 `Year` 明确交代（⚠️）、六维分项可做逐任务分析（与 P0-1 呼应）。
- 现在它在 `q4_data.py:61` 被读进内存后直接闲置——"已加载未使用"在代码审查里比"没加载"更难看。

### P0-3　C4 只用了 1/4 的必需字段

- 题目点名要"算力、数据量、开源权重"。实际只用了算力，且论文自己说 C4"主要用于覆盖审计"。
- `Training dataset size (total)` 未取 → 5,000B 参数的 D 上限设定没有外部参照；
- `Open model weights?` 未取 → "开源筛选口径"这一条要求连带无法交代；
- `C4 最大观测算力 5.000e26` 被直接当成前沿预测的起点，却没说是哪个模型、是否开源、什么时候发布的。

### P0-4　"贡献占比"没有给出，而它恰恰是本题的题眼

题目的通俗理解原话是"**把能力增长拆成两部分、分别计算规模扩张与非规模技术进步各自的贡献占比**"。目前只给了 `scale_sd_contribution=4.6850`、`time_sd_contribution=0.2221`，没有任何占比数字。

更麻烦的是：**这两个 sd 之比不是占比，而且换一种定义答案就翻脸**——

| 占比定义 | 规模扩张 | 非规模（时间） |
|---|---|---|
| 按 sd 之比（=论文隐含口径） | **95.5%** | **4.5%** |
| 按方差之比 | **99.78%** | **0.22%** |

同一个模型，同一组系数，一种定义说"非规模占 4.5%"，另一种说"占 0.2%"，差 20 倍。题目要"占比"，就必须**明确写出定义并给区间**。而我复算的 R²=0.3594 说明：两者合计只解释了 35.9% 的总方差，剩余 64.1% 归入残差——这个事实论文一个字没提。

同时，`q4_models.decompose_scale_vs_time` **没有输出任何标准误或置信区间**，而同一个包里的桥接拟合却老老实实做了 2000 次 bootstrap。核心结论（6.2244 vs 0.0670）连显著性都没检验，口径不一致。

---

## 三、P1：数值退化与外推合法性

### P1-1　前沿预测落在 Q3 可行域的**角点**上，整段在桥接支持域外

实查 `outputs/q3/q3_loss_frontier_curve.csv` 第 120 行（10^27 FLOP）：

```
N_params_B = 5000.0   D_tokens_B = 4000.0   loss = 1.356588
```

`N` 顶在 5000 上界、`D` 顶在 4000 上界，**两个边界同时激活**。Q4 的 6 个情景（6.25e26 → 8e27 FLOP）全部取自这条已经饱和的曲线末段，所以：

| 情景 | Loss* | 榜单预测 |
|---|---|---|
| 12 个月 × 1.25 | 1.3584 | 6.4740 |
| 12 个月 × 2.00 | 1.3566 | 6.4756 |
| 24 个月 × 4.00 | 1.3566 | 6.4756 |

**12 个月与 24 个月的预测差 0.0016 分，1.25× 与 4× 的算力差 32 倍而结果只差 0.0016 分。** 题目要的"12 / 24 个月前沿边界"实际上得到了同一个数——因为 Loss* 已经不是模型算出来的前沿，而是箱子角点的坐标。

再叠加桥接支持域：C6 High 组的 Loss ∈ **[2.0933, 2.5978]**，而预测点 Loss* = 1.357，**低出下界 0.74，约为全部观测跨度（0.50）的 1.5 倍**。论文的"支持域外"列 6/6 行全部非空——也就是说 Q4 的核心量化答案 100% 是外推，没有任何一行落在有校准数据的地方。

论文在 §5 用"支持域外"标注了这一点，但 §7 结论和摘要都没有明说"我们的数字来自可行域角点"。这是最该补的一句话。

### P1-2　桥接的**符号**都没被证实，却拿来做定量换算

`q4_bridge_summary.csv`：

```
slope = -0.8817   r2 = 0.1580   f_pvalue = 0.3773
slope 95%CI = [-4.4269, +0.5349]      ← 区间含 0
```

slope 的 95% 自助区间**跨过零点**，意味着"val_loss 越低、榜单分越高"这个方向在本样本里都未被证实。而 7 个 High 点本身还非单调（pythia-1.4b 的 Average 6.0085 > pythia-2.8b 的 5.5549），说明榜单噪声不小。

用它线性外推到 Loss=1.357，得到的预测值 6.4756，其 bootstrap 95% 区间是 **[4.9295, 9.5061]**——区间宽度 4.58 分，**比 C6 里全部实测 benchmark 的跨度（5.07→6.06，仅 0.99 分）宽 4.6 倍**。作为"能力前沿"交付，这个数的信息量接近于零。

**两个可选修法**（都比现状强）：
1. 换成有界/单调形式（logistic 或 `S = S_max − c·(L−L_min)^γ`），至少保证外推不发散；
2. **更彻底**：既然 Q3 的 `Loss*(C)` 在预测区间已经饱和，桥接这一步本身就是放大器而非信息来源——直接在 `S ~ log10(C) + month` 空间建前沿（C4 有 1390 条带算力的记录可用），把 Pythia 桥接降级为交叉校验。这样 Q4 的可交付数字会落在自己有数据的地方。

### P1-3　标度指数在 2.6 个数量级之外外推，且选的偏偏是偏离文献的那套

- B1 的 N **只有 8 个取值**，跨 0.0705 → 11.966 B（约 2.2 个数量级）；D 跨 0.134 → 299.893 B。
- Q3/Q4 一路外推到 **N = 5000 B（×418）**、**D = 4000 B（×13.3）**。
- 混合口径取的 α = **0.0661**，比 Chinchilla / Kaplan 的 ~0.34 **小 5 倍**；而另一套同源口径的 α = 0.2334，反而更接近文献。
- 题目只要求 Loss 口径可对照，代码却选择了物理上更可疑的 α。这个选择**直接决定**了"高预算 N 触顶"的结论：α 越小，N 的边际收益越小，最优解越被推向数据侧（或反向撞上别的边界）。

论文在 §3 已经诚实标注"该组参数须整体读取并标注为混合口径"，但没有讨论"α=0.0661 外推 418 倍"这个更本质的风险。

### P1-4　Q3 结论段把边界伪影写成了"规律性结论"

Q3 §7 原文：

> 主口径重算后，Q 恒卡在 0.95，质量维度的内点权衡不可识别；高预算 N 上界激活是边界设定和成本结构的共同结果。……由此得到的规律性结论是：**长上下文算力挤压高度非对称，应大幅削减参数规模而优先保数据吞吐**；该判断需用长文本训练计时和收益项补充实验验证。

前半句刚否定自己，后半句仍是明确的规范性表述。需要分开看：

- **"Q 恒卡 0.95"是伪影**——`q3_quality_cost_curves.csv` 的校准表显示：成本放大 1000 倍时质量成本占比也只到 0.648，直到 scale=3000 才跌出 0.95。也就是说，在题目附录 B 给定的指数型 g(Q)=10⁷e^{6Q} 下，"把质量拉满"几乎免费，这是**成本函数与系数标定的联合产物**，不是模型发现的规律。
- **"长上下文挤压参数规模"是站得住的**——在 10^22 档，Lctx 从 2048→32768 时 N 从 26.29B 降到 15.23B、D 从 58.40B 降到 51.59B，**是内点解不是角点解**（见表 4.3）。N 降 42%、D 只降 12%，"非对称"成立；Lctx_crit=6/η=30000 与题设公式一致。

所以修法不是删掉结论，而是**把"应大幅削减参数规模"从规范性主张降级为"在本代理模型与给定成本函数下"的条件性表述**，并明说 Q 维度不可识别。

---

## 四、P2：交付与排版（不影响得分逻辑，影响观感）

| # | 问题 | 证据 |
|---|---|---|
| 1 | **没有统一总摘要** | 文档是三篇论文首尾相接。最前面的摘要只覆盖第一问、第二问；第三问、第四问各自另起摘要与参考文献。四问一体的总摘要不存在 |
| 2 | **文件名与文内标题不一致** | 文件名"数学建模F题**完整**论文"，文内 Title 仍是"数学建模 F 题**统一**建模论文"，副标题"语料质量评分与大模型训练标度律"（只描述前两问） |
| 3 | **没有目录** | 全文检索"目录"只命中"输出目录" |
| 4 | **第三/四问用 Title 级样式** | `doc_get_outline` 返回空 —— 四级标题层级断裂，Word 导航窗格无法用 |
| 5 | **模板占位残留** | 第三问、第四问的标题下各留一行孤立的"数学建模 F 题"段落 |
| 6 | **桥接表被硬拆** | Q4 §3 的表被切成"表格续表"，`residual_rmse / slope_low / slope_high / intercept_low / intercept_high` 单独一行，阅读困难 |
| 7 | **参考文献有一条不是文献** | 第一篇第 5 条："Data Mixing Laws and RegMix related studies, with model comparison restricted to the supplied A and B attachments." —— 无作者、无年份、无出处，占了一个编号 |
| 8 | 第三问参考文献样式混乱 | 5 条全用 `[a]` 标记而非数字编号 |

---

## 五、仍然值得肯定的地方（不要因为上面的清单而低估）

1. **`q4_input_contract.json` 是这轮最有价值的产物**。它把 Loss 口径、桥接列名、混合系数来源、B9/B10 偏置量、Q̄ 插补损失、C8 排除策略全部固化成机器可读契约。上一轮我提的最大风险"Q2↔Q4 没有契约所以第四问必然拼错尺度"，被正面解决了。
2. **绝不掩饰不确定性**。R²=0.158、p=0.377、slope CI 含 0、bootstrap 区间宽到 4.58 分、"支持域外"逐行标注、"该偏差来自估算情景表，不是独立验证集"——这种写法在竞赛论文里非常少见，评审是会给分的。
3. **B9/B10 系统性偏置被量化并隔离**：128 个估算点 100% 残差为正、平均低估 0.3109 nats（95% CI 0.2992–0.3236），且明确"不叠加到中心预测、不作为已验证的物理校正"。诚实的正确做法。
4. **Q3 的混合口径现在处处标注**：§1、§3、摘要三处都写明"B1 经典项 + B6–B8 质量项，不是单一数据集联合拟合"。上一轮我提的"跨口径拼接"从隐患变成了显式声明。
5. **第一问的置换检验依旧是全场最漂亮的一步**：域内独立零分布给出 55.61% 的无冲突基准（该数值数学上精确正确），实测 A1/A2/A3 分别 42.55%/53.66%/47.91% 全部低于基准，于是推翻了"冲突普遍存在"的初判，改判"各维本质同向"。自我纠错 + 结论反转，这是真有含金量的工作。
6. **`q4_json_exclusion_manifest.csv`（440 KB）**：把 1958 个 JSON 的状态、4 个损坏文件的异常类型与行号全部留痕，可复现性做得扎实——只可惜用错了方向（见 P0-1）。

---

## 六、优先修复清单（按投入产出比排序）

| 优先级 | 动作 | 成本 | 收益 |
|---|---|---|---|
| **1** | 写一个脚本对 C8 做 `model × task` 聚合，输出各任务的时间斜率与规模斜率，写进 Q4 新一节 | 低（1 脚本 + 1 表 + 1 图） | 补上唯一被明文强制、且现在完全缺失的要求；同时给"非规模技术进步"提供任务级证据 |
| **2** | 改用 C3（`leaderboard_extended_timeseries.csv`）做 12/24 个月预测的时间轴，并交代"按 Year 对齐" | 低 | 一次性补齐"须使用 C3"+ 时间轴口径两项 |
| **3** | 从 C4 取 `Training dataset size (total)` 与 `Open model weights?`，新增"开源筛选口径"小节（是否须开源权重、许可证是否允许复现） | 低 | 补齐"须使用 C4 算力/数据量/开源权重"与"开源筛选口径"两项 |
| **4** | 重写 Q4 §4：给出**显式的贡献占比 + 定义 + bootstrap 区间**，并说明两者只解释 35.9% 方差 | 低 | 直接补上题眼要求，且顺带暴露"残差占 64%"，比现在的点估计更可信 |
| **5** | 在 Q4 §7 结论明写"预测值取自 Q3 可行域角点（N=5000/D=4000 双双触顶），且 Loss* 低于桥接支持域下界 0.74" | 极低 | 把最大的方法学弱点从"标注在表里"升级到"写在结论里"，避免被判为隐瞒 |
| **6** | 桥接换有界形式，或直接在 `S ~ log10(C) + month` 空间建前沿、把 Pythia 桥接降级为交叉校验 | 中 | 让 Q4 的核心数字落在有数据的地方，CI 从 ±2.3 收窄 |
| **7** | 补一份覆盖四问的统一总摘要 + 目录；文件名与文内标题统一为"完整论文"；第三/四问改 Heading 样式；清掉两处"数学建模 F 题"占位段 | 低 | 观感与结构完整性 |
| **8** | 删掉第 5 条伪文献，第三问参考文献改数字编号 | 极低 | 格式硬伤 |

---

## 七、总评

| 维度 | 评价 |
|---|---|
| 建模思路 | 好。四问递进链条清晰，Q1 置换检验、Q2 两阶段拟合切断截距-指数抵消、Q2 的 γ·Q_score^θ 保证 Q→1 退化经典式、Q4 的 High/Medium 分层桥接，都是有想法且正确的设计 |
| 代码与可复现性 | 好。全部数字可复算，接口有边界文件，原始数据不复制到输出目录，损坏数据留痕 |
| 学术诚信 | 优秀。不确定性标注的密度远超一般竞赛论文 |
| **答题合规性** | **差。题目附录 A 对问题四提了 3 条"须"式硬要求，只满足 1 条；C8 逐任务聚合被明文强制却完全未做** |
| 交付完整性 | 一般。无统一总摘要、无目录、标题层级断裂、文件名与内文不一致 |
| 结论可靠性 | 偏弱。Q4 的核心量化答案 100% 落在支持域外，且来自可行域角点，12/24 个月无区分度 |

**如果只能改三件事：C8 逐任务聚合、贡献占比显式化、结论里写明"角点解 + 支持域外"。** 前两件是补题目的硬要求，第三件是防止一个本来很诚实的作品因为结论里少写一句话而被判为隐瞒。
