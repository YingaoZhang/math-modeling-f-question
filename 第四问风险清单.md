# 第四问风险清单：前三问中「不改就会拖垮第四问」的全部位置

> 核查方式：读取 `src/f_question/` 全部代码 + `outputs/` 结果表 + 附件 C 原始文件（含 C8 逐任务 JSON），用项目自带 `.venv` 逐项实跑复现。所有数字均为实测值，非推断。

---

## 0. 第四问实际只从前三问拿两样东西

第四问的链路是：

```
算力预算 C ──[Q3 给出 Loss*(C)]──► 最优 Loss ──[C5/C6 桥接]──► Benchmark 前沿
                                                    │
                                    同时要拆：规模扩张 vs 非规模技术进步
```

展开后，前三问对第四问的**实际供给只有两项**：

1. 一个可信的 **Loss(N, D, Q, p)** 数值出口（来自 Q2 的标度律，经 Q3 优化成 `Loss*(C)`）；
2. 一个**与桥接表锚点一致的 Loss 尺度**（C5/C6 的高可比 Loss 明确锚定 Pythia，见下文）。

**现在这两项都有硬伤。** 下面是全部会传导到第四问的位置，按严重度排序。

---

## 1.【P0 致命】Loss 尺度三套并存，Q3 主口径与第四问桥接**完全不兼容**

### 实测的四套 Loss 尺度

| 来源 | 文件 | 覆盖范围 | Loss 实测区间 |
|---|---|---|---|
| Q1 The Pile（RegMix） | `outputs/q1/loss_metrics_by_domain_and_scale.csv` | 1m/60m/1B | 跨规模不可迁移 |
| Q2 B1（Pythia） | `q2_b1_predictions.csv` | Pythia ≤12B | 2.0933 – 4.7388 |
| Q2 B6–B8（半合成） | `q2_quality_predictions.csv` | 半合成 | 0.5000 – 3.7216 |
| **C5/C6 桥接（Q4 主用）** | `loss_benchmark_bridge*.csv` | 43 / 75 条 | **1.650 – 2.840**（High 组 2.093 – 2.598） |
| **Q3 主口径 b6_b8** | `q3_selected_by_budget_and_cost.csv` | 3 档预算 | **0.3556 – 1.3642** |
| Q3 对照口径 b1 | 同上 | 3 档预算 | 1.6739 – 3.3087 |

**《数据说明》原文（第 11–12 页）明确**：
> 「其中**高可比样本主要来自 Pythia（Loss 可与附件 B 对照）**，其余为中可比样本（Loss 来源于不同技术报告或论文）」

即：**第四问桥接的尺度锚点 = 附件 B 的 Pythia val_loss。**

### 致命之处

Q3 主口径 b6_b8 的三个 Loss 值 **0.3556 / 0.5947 / 1.3642，整段都低于桥接表的最低值 1.650**。
→ 拿 Q3 主口径的 Loss 去过桥，**三档预算全部落在桥接表支持域之外**，只能纯外推。

换成 b1 口径也只有一部分可用：

| Q3 情景 | Loss | 相对 C6 全量 (1.650–2.840) | 相对 C6 High 组 (2.093–2.598) |
|---|---|---|---|
| b1 @ 1e19 | 3.3087 | 高于上界 | 高于上界 |
| b1 @ 1e22 | 2.1296 | 域内 | **域内** |
| b1 @ 1e24 | 1.6739 | 域内（距下界仅 +0.024） | 低于下界 |
| b6_b8 @ 1e19 | 1.3642 | **低于下界** | **低于下界** |
| b6_b8 @ 1e22 | 0.5947 | **低于下界** | **低于下界** |
| b6_b8 @ 1e24 | 0.3556 | **低于下界** | **低于下界** |

> - 对 C6 **全量**域（1.650–2.840）：6 个取值里 **2 个**域内，且其中一个贴着下界 0.024；
> - 对 C6 **高可比组**域（2.093–2.598，即唯一可用于定标的子集）：6 个取值里 **只有 1 个**域内 —— `b1 @ 1e22`。
>
> 换句话说：**第四问唯一能用的那个 Loss 入口，来自 Q3 的对照口径，而且只在一个预算档上落进窗口。**

而 Q3 的**摘要、正文、§3 两套参数口径、§4 全部结果表、12 张图、§7 结论**，全部以 b6_b8 为主口径写成。

### 不改的后果
第四问拿不到一个「既有正当性、又能过桥」的 Loss 入口。用主口径 → 基准分预测系统性偏高；改用 b1 → 与 Q3 全文口径冲突，且只剩 1 个可用点。

### 修法
把 Q3 的 **Pythia 口径提升为对外主口径**（或新增一套以 B1 为基、质量项同源的完整口径并重出全部结论表）；同时输出一份显式的 **`Loss_scale = Pythia val_loss`** 声明，供 Q4 直接锚定。

---

## 2.【P0 致命】Q3 的算力覆盖够不到第四问的前沿，N 上界低于现实前沿

### 实测 C4（`epoch_all_ai_models.csv`，3523 行 × 57 列）

| 指标 | 实测值 |
|---|---|
| Training compute (FLOP) 有效样本 | 1390 |
| 中位数 / P90 / P95 / P99 / 最大 | 3.68e21 / 1.26e24 / 3.59e24 / 2.10e25 / **5.00e26** |
| **> 1e24 的模型数** | **164 个** |
| > 1e25 / > 1e26 | 21 / 3 |
| 现实最大（Grok 4，2025-07-09） | 5.00e26 |
| DeepSeek-V4-Pro（2026-04-24） | **9.702e24**，参数 1.6T |

### 与 Q3 的对比

- Q3 只算 **3 档**：1e19 / 1e22 / 1e24 → **最高预算 1e24 低于 DeepSeek-V4-Pro 的 9.702e24**
- Q3 的 N 上界：legacy **1000B** / expanded **2000B** → **低于 DeepSeek-V4-Pro 的 1600B**
- 第四问要预测 **未来 12/24 个月的前沿** → 需要 `Loss*(C)` 覆盖 **1e25–1e27**

### 不改的后果
Q3 的 `Loss*(C)` 曲线**连今天的前沿都没覆盖**。Q4 要么把曲线强行外推 1–3 个数量级（正好撞在人造 N 上界上，无任何支撑），要么整套重算。

另：`q3_selected_by_budget_and_cost.csv` 只有 3 个预算点，**没有输出连续的 `Loss*(C)` 曲线**（`q3_quality_cost_curves.csv` 是质量成本曲线，不是预算曲线）。Q4 需要的是连续函数。

### 修法
预算网格扩到 **1e25 / 1e26 / 1e27**；N 上界提到 ≥ 5e12（5000B）并给出依据；把 `Loss*(C)` 拟合成**连续曲线**（`q3_loss_frontier_curve.csv`）作为 Q4 的显式输入产物。

---

## 3.【P0 致命】Q3 的「最优 N」由人造边界决定 —— 直接污染第四问最核心的数字

### 实测证据

- **18/18 个已选情景 `Q_target = 0.95`（上界）** —— 质量维度没有内点权衡
- b1 @ 1e24 legacy：`N = 1000.000000`（正好贴 legacy 上界）
- b1 @ 1e24 expanded：`N = 1045.186161` —— **突破了 legacy 的 1000**，证明该「上界」是软的
- 主口径 b6_b8 @ 1e24：`N = 921.209622`（内点，**并未贴上界**）

### 为什么会重创第四问

第四问的核心任务恰是「**分离并分别量化规模扩张与非规模技术进步各自的贡献占比**」。拆解依赖规模弹性 α：

| 口径 | A | B | α | β |
|---|---|---|---|---|
| b1（Pythia） | 2.0824 | 1.2552 | **0.0661** | 0.2762 |
| b6_b8（主口径） | 0.8111 | 0.5641 | **0.2334** | 0.2625 |

**α 相差 3.5 倍**，直接导致同一算力下的最优 N 相差约 3 倍：

| 预算 | b1 给出 N | b6_b8 给出 N | 倍数 |
|---|---|---|---|
| 1e22 | 26.29 B | 81.06 B | 3.08× |
| 1e24 | 1000.00 B（贴界） | 921.21 B | 0.92× |

### 不改的后果
第四问最核心的那个数字（规模扩张贡献占比）会随口径选择剧烈摆动，做不出稳健结论。而题目要求「分离并分别量化」，这是硬指标。

### 修法
1. N 上界改成有文献/物理依据的边界并在论文中说明；
2. 给出 α 的口径选择依据，或对 α 做**双口径包络**（把 1.5–3 倍的摆动幅度做成区间报出来）；
3. 输出「最优 N 随 C 的连续曲线」而非 3 个离散点。

---

## 4.【P1 严重】Q2 在第四问需要的尺度上没有真实验证，且存在 **系统性低估 0.31 nats**

### 实测 `q2_large_scale_extrapolation.csv`（B9/B10，n = 128）

| 指标 | 实测值 |
|---|---|
| N 范围 | 100 – 10000 B（**含 100B 以上**） |
| `q2_large_scale_metrics.csv` R² | **0.3561** |
| 残差 min / max / mean / std | 0.2190 / 0.5592 / **0.3109** / 0.0710 |
| **残差为正的比例** | **100.0%** |

> **该标度律在 100B+ 尺度上一律低估 Loss，平均低估 0.3109 nats，无一个负残差。**

### 量级对比（这是关键）

C6 桥接表全部 75 条的 `Val_Loss` 跨度 = **1.650 – 2.840，只有 1.19 nats**。
→ **0.3109 nats 的系统性低估，吃掉桥接表整个 Loss 支持域的 26%。**

而第四问要做的事正是「把 Loss 外推到最大尺度再翻译成基准分」。这个偏差会**按原样变成 Q4 前沿预测的系统性乐观偏差**。

### 根因（可以直接写进论文，是个加分的自省点）

Q2 拟合的是**无截距**形式 `L = A·N^-α + B·D^-β`（README 原文：「拟合无截距标度律」），**缺少题目经典形式里的不可约损失 E**。小尺度上 E 被 A、B 吸收；大尺度上 `A·N^-α → 0`，缺失的 E 就以常数项形式暴露出来，表现为一致的负残差。

### 附带发现
`q2_scaling_validation_summary.csv` 中 **B2_Cerebras 的外族验证 R² = −5.06**（比直接用均值还差）。B4 R² = 0.585、B5 R² = 0.781 尚可，但那是**排序验证**（spearman 0.983 / 0.903），不能证明绝对 Loss 可用。

### 修法
1. **补截距 E 重拟合**，或对 B9/B10 做偏差校正并报告校正前后；
2. 明确把 B9/B10 标注为半合成（《数据说明》第 2 条明文要求：「非直接观测，应在论文中标注」），并把大尺度外推边界写进接口；
3. 给 Q4 一个「大尺度 Loss 预测的偏差区间」，而不是单点估计。

---

## 5.【P1 严重】Q1 已声明 Pile↔Pythia 的 Loss 桥接「不可识别」，但第四问恰恰需要跨口径

### `outputs/q1/interface_identifiability.csv` 原文

| analysis | status | reason（节选） |
|---|---|---|
| `A_ThePile_to_B_Pythia_val_loss_bridge` | **not_identifiable_from_available_pairing** | 「The validation corpora differ and no common paired observations are supplied; **no affine or monotone bridge is fit**.」 |
| `A_Q_to_B_Q_score_rescaling` | not_identifiable_from_available_pairing | 无同文档配对观测 |
| `supervised_meta_rater_weights` | not_identifiable_from_available_pairing | A16 只覆盖 **6/17** 域 |

Q1 不虚构拟合式，这是**很诚实、很值得保留的做法**。但它带来一个后果：

> **从前三问走到第四问，中间那一段「Loss 尺度对齐」的桥，目前是空的。**

而 `outputs/q1/q2_input_interface_bundle.json` 只写了：

```json
"loss_scale_note": "The Pile validation cross entropy only; do not mix directly with Pythia val_loss"
```

→ 只声明了 **Q1 ↔ Q2** 的隔离，**完全没有留下 Q2 ↔ Q4 的契约**。

### 不改的后果
Q4 只能自己臆造桥接锚点，几乎必然拼错尺度（见第 1 条）。

### 修法
新增 `outputs/q4_input_contract.json`（或等价接口文档），至少写清四项：
1. Q4 唯一允许使用的 Loss 口径 = **Pythia val_loss**；
2. 该口径的来源文件与列名（`q2_b1_fit_coefficients.csv` / Q3 的 b1 情景）；
3. 与 C5/C6 桥接的哪一列对齐（`Val_Loss`）；
4. 已知偏差及其区间（即第 4 条的 0.31 nats）。

---

## 6.【P2 中等】Q2 的质量项与经典项是跨口径拼接，Q3 自称禁止却照做了

### Q3 报告 §摘要 原文
> 「主口径使用 B6-B8 λ=10 的整组六参数，B1 的整组 A、B、α、β 与**同一质量项**作为对照，**禁止跨口径拼接**」

### `src/f_question/q3_data.py::_read_coefficients` 的代码注释原文
> 「``b1`` keeps B1's jointly fitted ``(A, alpha)`` and ``(B, beta)`` as a sensitivity baseline; **its quality term is paired with the same lambda=10 B6--B8 fit because B1 contains no native quality coefficient**.」

即 **b1 口径 = B1 的 (A, B, α, β) + B6–B8 的 (γ, θ)** —— 正是它自称禁止的那种拼接，只是换了个说法（「对照口径」）。

而第 1 条的结论要求 **Q4 必须用 b1 口径才能过桥**，于是这个未声明的拼接会一路传进第四问。

### 缓解事实（用得上）
`q2_quality_effect_coefficients.csv` 显示 γ、θ 对 λ 几乎免疫：

| λ | γ | θ |
|---|---|---|
| 0.0 | 1.516282 | 1.087229 |
| 0.1 | 1.516312 | 1.087182 |
| 1.0 | 1.516308 | 1.087177 |
| 10.0 | 1.516492 | 1.086828 |

γ 波动 < 0.02%，θ 波动 < 0.04%。**拼接风险在数值上其实可忽略**，但必须在论文里显式声明来源，不能一边写「禁止跨口径拼接」一边在代码里拼接。

### 修法
把 b1 口径改名并显式标注为「**混合口径（A/B/α/β 来自 B1，γ/θ 来自 B6–B8）**」，同时引用上表说明 λ 不敏感性，把风险显式消掉。

---

## 7.【P2 中等】Q1 的 Q 有 14/17 是中位数插补（方差损失 91.4%），会削弱第四问的「技术进步」归因

### 实测 `qbar_imputation_variance_loss.csv`
- 17 个域中仅 **3 个**有文档级质量观测，**14 个为中位数插补**
- 方差损失 **91.4%**

Q3 的 `Q_eff = 1 − (1−Q)·Q0/Q̄(p)` 依赖 Q̄(p)，因此 Q3 自己就承认「Q̄(p) 只作为弱证据」。而 `interface_identifiability.csv` 又记录 A16 映射**只覆盖 6/17 域**。

### 为什么影响第四问
第四问要把能力增长拆成「规模扩张」与「非规模技术进步」，**数据工程/数据质量是技术进步的主要来源之一**。若 Q 有 82% 是插补值，Q4 无法把技术进步归因到质量维度，只能把非规模部分整体做成黑箱残差 —— 而题目要求「**分离并分别量化**」，黑箱残差拿不到分。

### 修法
把 A16 映射真正扩到 17/17 域（附件 A 的 `domain_mapping_guide.csv` 可用）；或在 Q4 中明确剔除质量维度并给出理由，都比默默用一个 91.4% 方差损失的 Q 要好。

---

## 8.【P2 中等 · 接口预留】C6 桥接的可用性极低，前三问没有为它留任何铺垫

这一条严格说是第四问自己的活，但**前三问现在没有预置任何接口**，会导致 Q4 的方法设计从第一步就走偏。实测：

### C6（`loss_benchmark_bridge_expanded.csv`，75 条）分层

| 组 | n | N 范围 | Val_Loss | LB_Average |
|---|---|---|---|---|
| High（Pythia，可与附件 B 对照） | **7** | **0.16 – 12.0 B** | 2.093 – 2.598 | **5.07 – 6.06（几乎不变）** |
| Medium | 68 | 0.49 – 72.7 B | 1.650 – 2.840 | 3.80 – 47.98 |

**三个硬问题：**

1. **高可比组只有 7 个点，N 只到 12B**，而 Q4 的前沿在 100B–1.6T → 桥接需要外推 N **1.5 个数量级以上**；
2. **High 组的 LB_Average 几乎不变（5.07–6.06，跨度 1 分）**，`corr(Val_Loss, LB_Average) = −0.3975`（n=7，**不显著**）；全量 75 条也只有 −0.51（R² ≈ 0.26）；
3. **Medium 组 68 条来自 30 种不同来源**（Qwen2.5 report / Gemma2 report / Phi-3 / Falcon / LLaMA 2 …），其中**大量是 Instruct/Chat 变体**：`Qwen/Qwen2-0.5B-Instruct`、`01-ai/Yi-1.5-34B-Chat`、`Qwen/Qwen2.5-14B-Instruct-1M` …
   → **Loss 来自 base 模型、Benchmark 来自 instruct 模型，配对不对**。而题目明文要求「**模型类型须区分 pretrained 与 chat/finetuned**」。

### C1 榜单的类型分布也证实这个坑

`leaderboard_cleaned.csv`（4576 行）：

| Type | 行数 |
|---|---|
| fine-tuned on domain-specific datasets | 1785 |
| base merges and moerges | 1724 |
| chat models (RLHF, DPO, IFT, …) | 718 |
| **pretrained** | **275**（仅 6%） |
| continuously pretrained | 58 |
| multimodal / other | 16 |

若按题目要求严格筛 pretrained，只剩 **275 行**；且 **Submission Date 只有 2024-06-08 ~ 2025-03-13（约 9 个月）**，比「当前」（2026-09）早 18 个月。

### 修法（属 Q4，但要在前三问的接口里预留）
在接口契约中明确：**桥接只允许用 High 组做定标，Medium 组仅用于排序方向核对**；并预先声明 Benchmark 端的能力度量定义与开源筛选口径（用 `Hub License` 还是 `C4 的 Open model weights?`——后者覆盖 2653/3523，而 C2 的 `Epoch_AI_Open_Weights` 只覆盖 443/4576，仅 9.8%）。

---

## 9.【P3 中等】交付与复现链已断裂，会影响第四问的并入

| 问题 | 实测 |
|---|---|
| 论文分成两套 | `outputs/数学建模F题统一论文.docx`（只含 Q1+Q2）与 `outputs/q3/第三问完整论文.docx` 并存 |
| README 口径过期 | 仍写「本项目完成数学建模 F 题**两问**」 |
| 构建脚本不含 Q3 | `build_unified_docx.py` 只汇总 Q1/Q2 |
| **硬编码绝对路径** | README 的构建命令写死 `C:\Users\admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe` → 换机必失败 |

### 修法
README 改成环境无关（`python build_unified_docx.py`）；把 Q3 并入统一论文；给 Q4 留章节位。这条成本最低，但**不修则 Q4 复现的第一步就断**。

---

## 10.【P3 小】两处表述与自身数据不符（会被评委抓）

| 位置 | 原文 | 事实 |
|---|---|---|
| Q3 §4.3 | 「η=2×10⁻⁴ 为**自设**简化系数」 | η=2×10⁻⁴ 是**题目明确给定**的（题目原文：「其中 η=2×10−4」） |
| Q3 §摘要 | 「高预算 **N 触及人为上界**」 | 主口径 b6_b8 @1e24 = **921.2B**（内点）；只有对照口径 b1 才恰好 `1000.000000`。表述与自身数据不符 |

另：Q3 结论段「**应大幅削减参数规模而优先保数据吞吐**」是从「Q 恒贴 0.95 + N 贴界」这种退化解里推出的因果性表述。Q3 正文已诚实承认是边界伪影，但结论段又当成了规律 —— 建议把结论段改成条件性表述（「在当前成本校准下」），否则第四问若引用会被连带质疑。

---

## 修复优先级总表

| 优先级 | 项 | 为什么排这里 | 预估工作量 |
|---|---|---|---|
| **P0** | 1. Loss 尺度统一到 Pythia 口径 | 不修就没有可用的 Loss 入口，Q4 无法开始 | 中（重出 Q3 全部结论表） |
| **P0** | 2. 算力网格与 N 上界扩到前沿 | 不修则 Q4 全部结论都在支持域之外 | 小（改 `BUDGETS` 与 bounds 常量） |
| **P0** | 3. 去边界伪影 + 给定 α 口径依据 | 直接决定 Q4 最核心的数字 | 中 |
| **P1** | 4. 补截距 E，消掉 0.31 nats 系统偏置 | Q4 前沿预测系统性乐观偏差的根源 | 中 |
| **P1** | 5. 补 Q2↔Q4 的 Loss 口径契约 | 防止 Q4 拼错尺度 | 小 |
| **P2** | 6. 显式声明 b1 的混合口径 | 诚信风险，改起来最快 | 很小 |
| **P2** | 8. 桥接分层规则前置到接口 | 影响 Q4 的方法设计走向 | 小 |
| **P3** | 7. A16 映射扩到 17/17 域 | 影响技术进步归因粒度 | 中 |
| **P3** | 9 / 10. 文档合并 + 表述修正 | 收尾必做，成本低 | 很小 |

---

## 附：一句话总结

> 前三问本身的建模质量是过关的（所有关键数字我都能复现，无编造、无前后矛盾）。但**它把「Loss 口径」当成内部细节处理了，而没有把它当成跨问契约来维护** —— 于是第四问要接的时候，会发现：主口径的 Loss 过不了桥、能过桥的口径又不是主口径、而且那个唯一可用的口径本身在目标尺度上有 0.31 nats 的系统性低估。**第 1、2、3 条必须先修，第四问才站得住。**

---

*核查时间：2026-09-25 · 核查范围：`D:\project` 全部代码、结果表与附件 C 原始文件*
